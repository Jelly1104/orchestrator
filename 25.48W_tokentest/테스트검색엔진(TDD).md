- demo ver.002_251127
  - 서비스: **테스트검색엔진\_ver02**
  - 대상: **ver01 (MVP)**
    - 텍스트/이미지/혼합 입력 → Lexical/Vector Summary
    - Hybrid Search API (BM25 + Vector, BM25는 간단한 텍스트 검색 수준이어도 됨)
    - 쿼리 임베딩 캐시 + 토큰 제한 로직
    - 추천, 고급 분석 등은 **범위 밖**
  - 이 TDD의 목적:
    - **이미 구현된 것 = “Done” 표시**
    - **남은 것 = 4시간 안에 구현 가능한 핵심 테스트만 정의**
    - ver01에서 우선:
      - 멀티모달 요약 API 안정화
      - 이후 검색/임베딩 쪽로 확장 가능한 토대 준비
  ***
  ## ✅ 1. Summary 모듈 (M01, M02)
  ### **M01 – 텍스트 요약 모듈**
  - [x] 텍스트 입력 시 GPT 요약 호출 기능 구현됨
  - [x] 텍스트 요약 모듈 유닛 테스트
  - [x] 오류 케이스(mock 실패 등) 테스트
  ### **M02 – 이미지/혼합 요약 모듈**
  - [x] 이미지 업로드 처리(preprocess) 구현됨
  - [x] 이미지+텍스트 혼합 입력 → GPT 요약 호출 구현됨
  - [x] 이미지 처리/혼합 입력 테스트
  - [x] **요약 API(mock 실패 처리) 통합 테스트 코드 구현**
  - 텍스트/이미지/혼합 입력에서 GPT 호출 실패, 이미지 처리 실패, base64 변환 실패 등
    **모든 내부 오류(mock) 상황에서 API가 4xx + 명확한 메시지로 응답하는지 검증**
  ***
  ## 🟨 2. Token 관리 모듈 (M03)
  ### **M03 – 토큰 제한 및 재요약**
  - [x] token_count 계산 함수 구현
  - [x] limit 이하 텍스트는 그대로 반환 테스트
  - [x] limit 초과 텍스트는 1차 축약 수행 테스트
  - [x] 축약 후에도 limit 초과 시 2차 aggressive 요약 테스트
  - [x] LLM 호출(mock) 검증
  ***
  ## 🟨 3. Embedding Wrapper + 캐싱 (M04)
  ### **M04 – 쿼리 임베딩 캐시**
  - [x] 캐시 미스 시 임베딩 API 호출 테스트
  - [x] 캐시 히트 시 임베딩 API 미호출 테스트
  - [x] 캐시에 embedding 저장 검증
  - [x] 비정상 입력(빈 문자열) 처리 테스트
  - [x] embedding wrapper 기본 구현
  ***
  ## 🟨 4. Hybrid Search 로직 (M05)
  ### **M05 – BM25 + Vector 결과 병합**
  - [x] bm25-only, vector-only 병합 테스트
  - [x] 중복 doc-id 병합(1개만 노출) 테스트
  - [x] 가중치 기반 score 계산 테스트
  - [x] hybrid_merge 기본 구현
  ***
  ## 🟩 5. Summaries API (API01)
  ### **API01 – `/api/v1/summarize`**
  - [x] 텍스트 요약 API 구현
  - [x] 이미지 요약 API 구현
  - [x] 혼합 요약 API 구현
  - [x] 텍스트-only 요청 200 응답 테스트
  - [x] 이미지-only 요청 200 응답 테스트
  - [x] 텍스트+이미지 혼합 요청 테스트
  - [x] 필수 파라미터 누락 4xx 테스트
  ***
  ## 🟥 6. Search API (API02)
  ### **API02 – `/search` (스켈레톤 설계, ver02 이후 개발)**
  - [x] `/search` API 스켈레톤 생성
  - [x] 최소 쿼리 처리 로직 작성
  - [x] 잘못된 요청 4xx 테스트
  - [x] 캐시 히트/미스 플로우 연결 테스트
  - [x] hybrid_merge 연결 테스트
  ***
  ## 📦 7. 개발 진행 체크리스트 (전체)
  ### **완료된 항목**
  - [x] 멀티모달 요약 전체 플로우 구현
  - [x] 이미지 preprocess & Base64 변환
  - [x] GPT-5-mini 호출 모듈 구현
  - [x] Next.js → FastAPI end-to-end 요약 연결
  ### **앞으로 진행해야 할 ver01 작업**
  - [x] M03 토큰 관리 모듈
  - [x] M04 임베딩 wrapper + 캐시
  - [x] M05 Hybrid Search 스켈레톤
  - [x] `/summaries` API 테스트 완성
  - [x] `/search` API 스켈레톤 + 테스트
  - [x] pytest 환경 세팅
  - [x] mock 기반 LLM/embedding 테스트 유틸 작성
  ***
  ## 🕒 8. 4시간 구현 플랜 (체크박스)
  ### **1시간 – TDD 초안 기반 스켈레톤**
  - [x] M03/M04/M05 인터페이스 생성
  - [x] `/search` 스켈레톤 생성
  - [x] pytest 세팅
  ### **1.5시간 – 핵심 모듈 구현 + 테스트**
  - [x] M03 구현 + 테스트
  - [x] M04 구현 + 테스트
  - [x] hybrid_merge 단순 구현 + 테스트
  ### **1시간 – API 테스트 완성**
  - [x] summarize API 테스트 통과
  - [x] `/search` 최소 기능 테스트 통과
  ### **0.5시간 – 정리**
  - [x] 코드 정리
  - [x] 주석/README 업데이트
  ***
- demo ver.003_251128
  # TDD – 테스트검색엔진\_ver03
  - 서비스: **테스트검색엔진\_ver03**
  - 대상: **ver03 (ver02 확장)**
    - 기존 기능:
      - 텍스트/이미지/혼합 입력 → Lexical/Vector Summary
      - Hybrid Search API (BM25 + Vector)
      - 쿼리 임베딩 캐시 + 토큰 제한
    - ver03에서 추가되는 핵심:
      - **Lexical / Vector 요약 모델 다중 선택**
      - **모델별 token limit 설정 + min/max 가드**
      - **요약 multi-step 과정 + 토큰 수 디버그**
      - `/search` 응답에 **debug 패널용 데이터 포함**
  - 이 TDD의 목적:
    - ver03에서 **4시간 안에 핵심만 구현할 수 있는 테스트** 위주 정의
    - 특히:
      - 모델 레지스트리 + limit 가드
      - multi-step 요약/토큰 디버그
      - 확장된 `/search` API 응답 구조 검증
  ***
  # 🟨 7. ver03 신규 모듈
  ## 🟨 7-1. Summary Model Registry & Token Limit (M06)
  ### **M06 – 요약 모델 레지스트리 + Token Limit 가드**
  ### 1) 모델 레지스트리 로딩
  - [x] `SUMMARY_MODELS` 레지스트리를 관리하는 모듈 구현
    - `app/core/summary_models.py`
  - [x] `get_model_config(model_id)` 함수 테스트
    - 존재하는 model_id → config dict 반환
    - 존재하지 않는 model_id → KeyError 또는 커스텀 예외 발생
  ### 2) 모델별 token limit 범위 관리
  - [x] `clamp_limit(model_cfg, user_limit)` 함수 구현
    - user_limit가 `min_limit` 미만 → `min_limit`으로 보정
    - user_limit가 `max_limit` 초과 → `max_limit`으로 보정
    - user_limit가 None → `default_limit` 반환
  - [x] 잘못된 형식의 limit (음수, 문자열 등) 입력 시 예외 처리 테스트
  ### 3) API 연동 관점 테스트 (단위 수준)
  - [x] `/search` 요청 payload에 `lexical_model_id`, `vector_model_id`, `lexical_limit`, `vector_limit`가 들어왔을 때:
    - 존재하지 않는 model_id → 4xx 응답 테스트(invalid model_id → 422, invalid limit 타입 → 422)
    - limit가 모델 허용 범위 밖 → clamp 됐는지 확인 (mock 기반)
  ***
  ## 🟨 7-2. Multi-step Summary + Token Debug (M07)
  ### **M07 – summarize_with_steps 파이프라인**
  ### 1) 기본 동작
  - [x] `summarize_with_steps(text, limit, shorten_fn, counter_fn)` 함수 구현
    - 반환: `(final_summary, steps_list)`
    - steps_list 원소 구조: `{ "summary": str, "tokens": int }`
    - `app/core/summarizer.py` :`summarize_with_step`
  - [x] limit 이하 텍스트일 때:
    - steps에 최소 1개(원문)만 기록
    - shorten_fn 호출되지 않음 (mock로 검증)
  ### 2) limit 초과 시 multi-step 동작
  - [x] 1차 요약 호출 테스트
    - counter_fn 결과가 limit 초과 → shorten_fn이 1회 호출
    - steps에 2개 이상(원본 + 요약1) 기록
  - [x] 2차 aggressive 요약 호출 테스트
    - 1차 요약 후에도 limit 초과 → shorten_fn이 aggressive=True로 한 번 더 호출
    - steps에 3개 이상 기록
  - [x] shorten_fn mock으로 호출 인자(aggressive 플래그) 검증
    - 첫 호출: aggressive=False
    - 두 번째 호출: aggressive=True
  ### 3) steps 정합성
  - [x] steps 리스트가 시간 순서대로 정렬되어 있는지 테스트
  - [x] 각 step의 tokens 값이 counter_fn의 결과와 일치하는지 검증
    > 여기까지는 모델과 무관한 “generic multi-step 요약/토큰 파이프라인”에 대한 TDD.
  ***
  ## 🟨 7-3. Summary Models + Pipeline 연결 (M08)
  ### **M08 – Lexical/Vector Summary 파이프라인 분리**
  ### 1) shorten_fn 팩토리
  - [x] `make_shorten_fn(model_id, style)` 함수 구현
    - style: `"lexical"` 또는 `"vector"`
    - 내부에서 LLM 호출 대신 mock 가능한 함수 사용 (테스트에서 monkeypatch)
  - [x] model_id / style 조합에 따른 shorten_fn 선택 테스트
    - lexical_model_id로 생성한 shorten_fn이 lexical 스타일 프롬프트를 사용했는지(간단한 식별 문자열로 검증)
    - vector_model_id로 생성한 shorten_fn이 vector 스타일 프롬프트를 사용했는지 검증
  ### 2) Lexical/Vector Summary 각각에 대해 summarize_with_steps 적용
  - [x] `build_lexical_pipeline(query, model_id, limit)`이:
    - summarize_with_steps를 호출하고
    - `{"final_summary", "final_tokens", "steps": [...]}` 구조를 반환하는지 테스트
  - [x] `build_vector_pipeline(query, model_id, limit)`도 동일 구조 테스트
  ***
  ## 🟨 7-4. Summary Models API (API03)
  ### **API03 – `/api/v1/summary-models`**
  ### 1) 모델 목록 제공
  - [x] GET `/api/v1/summary-models` 요청 시:
    - 200 응답
    - `models` 필드가 리스트 형태
    - 각 모델에 `id`, `label`, `min_limit`, `max_limit`, `default_limit` 포함 여부 테스트
  ### 2) 프론트 안내용 데이터 검증
  - [x] `min_limit <= default_limit <= max_limit` 보장하는지 테스트
  - [x] 모델 타입(`type: lexical/vector`)이 함께 내려오는지 테스트 (있으면 프론트에서 분류 가능)
  ***
  ## 🟨 7-5. Search API ver03 확장 (API02-v3)
  ### **API02-v3 – `/api/v1/search` (debug + 모델/limit 반영)**
  ### 1) 요청 스펙 테스트
  - [x] 정상 요청:
    - `query`, `lexical_model_id`, `vector_model_id`, `lexical_limit`, `vector_limit` 포함
    - 200 응답 + `results` + `debug` 필드 존재
  - [x] 잘못된 요청:
    - query 공백 → 422
    - 존재하지 않는 model_id → 4xx
    - limit가 비정상 문자열 → 4xx 또는 기본값 fallback (정책에 맞춰 테스트)
  ### 2) debug 구조 테스트
  - [x] 응답의 `debug.input.tokens`가 M03 `token_count(query)`와 일치하는지
  - [x] `debug.lexical` 구조:
    - `final_summary`, `final_tokens`, `steps` 필드 존재
    - steps 배열의 각 요소가 `{summary, tokens}` 형태
  - [x] `debug.vector` 구조: lexical과 동일한 패턴 검증
  ### 3) 파이프라인 연결 테스트 (mock 기반)
  - [x] `summarize_with_steps`를 mock하여:
    - `/search` 요청 시 lexical/vector 두 번 호출되는지
    - 각각에 올바른 model_id, limit이 전달되는지 검증
  - [x] `hybrid_merge`와 `get_query_embedding`이 이전처럼 호출되는지 (ver02 테스트 재활용)
  ***
  # 📦 8. ver03 개발 진행 체크리스트 (전체)
  ### **완료된 항목 (ver01/ver02)**
  - [x] M01/M02 요약 모듈
  - [x] M03 토큰 관리
  - [x] M04 임베딩 wrapper + 캐시
  - [x] M05 Hybrid Search
  - [x] `/summarize` API
  - [x] `/search` API (기본 버전)
  - [x] pytest 환경 및 mock 유틸
  ### **ver03에서 새로 구현해야 할 항목**
  - [x] M06 Summary Model Registry + Token Limit 가드
  - [x] M07 multi-step 요약 + 토큰 디버그 파이프라인
  - [x] M08 Lexical/Vector 파이프라인 분리 + shorten_fn 팩토리
  - [x] API03 `/summary-models`
  - [x] API02-v3 `/search` debug 확장
  ***
  # 🕒 9. ver03 4시간 구현 플랜 (체크박스)
  ### **1시간 – 모델/limit 레지스트리 스켈레톤**
  - [x] summary_models 모듈 생성 (`SUMMARY_MODELS`, get_model_config, clamp_limit)
  - [x] `/summary-models` API 스켈레톤 + 기본 테스트
  ### **1.5시간 – multi-step 요약/디버그 파이프라인**
  - [x] summarize_with_steps 구현 + 테스트 (M07)
  - [x] lexical/vector 파이프라인 빌더 + shorten_fn 팩토리 (M08)
  ### **1시간 – `/search` 확장 + 통합 테스트**
  - [x] `/search`에 모델/limit 적용 + debug 포함
  - [x] mock 기반으로 summarize_with_steps, hybrid_merge, embedding 호출 검증
  ### **0.5시간 – 정리**
  - [x] TDD 기준 구현 범위 확인
  - [x] README/기술설계서 ver03 반영

# TDD – 테스트검색엔진\_ver04 (Batch Evaluation)

- 서비스: **테스트검색엔진\_ver04**
- 대상: **배치 평가/리포트 생성 전용 기능**
  - 입력: `content_summary_100_samples.json`
  - 처리: Lexical/Vector 요약 생성, tiktoken 기반 토큰 측정, 키워드 보존율 계산
  - 출력: per-document 평가 CSV + 종합 리포트 Markdown
- 목적:
  - ver03의 요약 파이프라인이 “실제 데이터셋 100개”에서 일관되고 안정적으로 작동하는지 검증
  - 토큰 감소율·키워드 보존율을 정량화
  - 이후 ver05 검색엔진 본 개발을 위한 품질 기준 확보

---

# 🟦 1. 배치 입력 처리 (M10)

### **M10 – JSON 로딩 및 스키마 검증**

- [x] JSON 파일 로딩 함수 구현
- [x] 각 row가 다음 필드를 포함하는지 테스트
  - `content_id`
  - `title`
  - `content_summary`
- [ ] 결측/빈 문자열 → 스킵 또는 경고 처리 테스트
- [x] 100개 문서 전체를 list로 반환하는지 테스트

---

# 🟦 2. 토큰 계산(tiktoken) (M11)

### **M11 – token_count_tiktoken()**

- [x] `o200k_base` 인코딩 로딩 테스트
- [x] 짧은 문장에 대해 예상 토큰 수와 일치하는지 테스트
- [x] 빈 문자열 처리 (`0` 반환) 테스트
- [x] 대량(100건) 배치 적용 시 예외 발생 없는지 테스트

---

# 🟦 3. 요약 파이프라인 재사용 (M12)

### **M12 – Lexical/Vector Summary 배치 적용**

- [x] ver03 `summarize_with_steps()`를 그대로 호출 가능해야 함
- [x] lexical summary 최종본이 문자열로 반환되는지 테스트
- [x] vector summary 최종본이 문자열로 반환되는지 테스트
- [x] default_limit 사용 시 limit 범위 적용(clamp) 테스트
- [x] LLM 호출 mock으로 대체하여 배치 안정성 테스트

※ ver04에서는 “요약 품질 평가”가 목적이므로

프론트용 디버그(steps)는 리포트에 포함하지 않음.

---

# 🟦 4. 키워드 보존율 계산 (M13)

### **M13 – keyword_coverage()**

- [x] 원문 키워드 집합 추출 테스트
- [x] lexical 요약 키워드 집합 추출 테스트
- [x] coverage 계산:
  - `len(KW_orig ∩ KW_lex) / len(KW_orig)` 검증
- [x] 1글자 토큰 제거, 특수문자 제거, lower-case 정규화 테스트
- [x] edge case:
  - KW_orig = 0 → coverage = 1.0 처리
- [x] coverage < 0.6 → `LOW_COVERAGE` flag 생성 테스트

---

# 🟦 5. 결과 리포트 생성 (M14)

### **M14 – Report Generator**

### 1) per-document CSV 생성

- [x] 아래 컬럼이 모두 생성되는지 테스트:

| content_id | orig_tok | lex_tok | vec_tok | lex_cov | flags |

- [x] 100개 row 생성되는지 테스트
- [x] float(coverage) 반올림/포맷 통일 테스트

### 2) summary Markdown 생성

- [x] 전체 샘플 수/평균/중앙값/최소/최대가 포함되는지
- [x] 키워드 보존율 통계 포함되는지
- [x] flag 빈도 집계 포함되는지
- [x] Markdown 파일 정상 저장 테스트

---

# 🟦 6. 전체 플로우 통합 테스트 (M15)

### **M15 – end-to-end 배치 실행**

- [x] 입력 JSON → 요약 생성 → 토큰 측정 → coverage 계산 → 리포트 출력까지 오류 없이 실행되는지
- [x] 오류 발생 문서는 스킵되지 않고 flags 처리되는지
- [x] 실행 후 CSV/Markdown 파일이 생성되는지 테스트
- [x] 배치 처리 시간 1~3초 이내(LLM mock 기준)

---

# 🟩 7. ver04 체크리스트

- [x] M10 JSON 로더 + 스키마 검증
- [x] M11 tiktoken 토큰 카운터
- [x] M12 lexical/vector 요약 파이프라인 mock 테스트
- [x] M13 keyword coverage 계산기
- [x] M14 리포트 생성기
- [x] M15 end-to-end 테스트

---

# 🕒 ver04 4시간 작업 플랜

### **1시간 – 데이터 로더 & tiktoken**

- [x] JSON 로더
- [x] token_count_tiktoken()

### **1시간 – 요약(mock) + coverage**

- [x] summarize_with_steps mock 연결
- [x] keyword coverage 계산

### **1시간 – CSV/Markdown 리포트**

- [x] Reporter 구현
- [x] Unit Test 작성

### **1시간 – 전체 배치 실행**

- [x] end-to-end 테스트
- [x] 샘플 100개에 대해 리포트 생성

---

- demo ver.005_251201
  # TDD – 테스트검색엔진\_ver05 (LLM Eval + Preview/Download)
  - 서비스: **테스트검색엔진\_ver05**
  - 대상:
    - ver04 배치 평가 파이프라인을 **LLM 실제 요약 + 재시도 + 프론트 Preview/다운로드**까지 확장
  - 주요 기능:
    - LLM 기반 Lexical/Vector 요약 wrapper (retry/timeout/fallback)
    - `/api/v1/eval/preview`로 JSON 업로드 → 요약·토큰·coverage 결과 반환
    - `/api/v1/eval/download`로 CSV/MD 파일 다운로드용 payload 제공
    - 기존 ver04 metrics/report 재사용
  ***
  ## 🟦 1. LLM 요약 Wrapper (M16)
  ### M16 – summarize_lexical / summarize_vector
  - [ ] `summarize_lexical(text, limit, model, retries)` 함수 구현
  - [ ] `summarize_vector(text, limit, model, retries)` 함수 구현
  - [ ] 두 함수 모두 프롬프트에 `{limit}` 포함하는지 테스트
  - [ ] 정상 응답 시 string 반환 테스트
  - [ ] LLM 클라이언트를 mock해서 200 응답 시 첫 시도에 성공하는 케이스 테스트
  - [ ] 429/500/timeout 등에서 최대 N회까지 재시도하는지(mock 호출 횟수) 테스트
  - [ ] N회 실패 시 `"SUMMARY_FAIL"` 또는 지정된 fallback 문자열 반환 테스트
  - [ ] 실패 시 로그/flag용 정보(예: 마지막 에러 메시지) 반환/저장 여부 테스트
  ***
  ## 🟦 2. Eval Preview API (M17)
  ### M17 – `/api/v1/eval/preview`
  - [ ] POST `/api/v1/eval/preview` 기본 성공 케이스
    - 유효한 `samples` 배열 + `lexical_limit` + `vector_limit` + 모델명 전달
    - 200 응답
    - `rows`와 `summary` 필드 존재
  - [ ] rows 스키마 테스트:
    - 각 row에 `content_id`, `orig_tok`, `lex_tok`, `vec_tok`, `lex_cov`, `flags` 포함
  - [ ] summary 스키마 테스트:
    - 샘플 수, 평균/최소/최대 토큰, 평균 coverage 등 필드 존재
  - [ ] invalid JSON 구조(리스트 아님/필수 필드 누락) → 4xx 응답 테스트
  - [ ] limit가 숫자가 아닐 때 → 4xx 또는 기본값 fallback 동작 테스트(정책에 맞춰)
  - [ ] LLM wrapper(mock) 연결 테스트:
    - preview 호출 시 `summarize_lexical`, `summarize_vector`가 기대 횟수만큼 호출되는지
    - limit/model 파라미터가 wrapper에 그대로 전달되는지
  - [ ] LLM 실패(`"SUMMARY_FAIL"`)가 발생한 row에 `LLM_FAIL` flag가 붙는지 테스트
  - [ ] coverage < threshold인 row에 `LOW_COVERAGE` flag가 포함되는지 테스트
  ***
  ## 🟦 3. Download API (M18)
  ### M18 – `/api/v1/eval/download`
  - [ ] POST `/api/v1/eval/download` 성공 케이스:
    - 요청 바디에 `rows` 또는 `samples + 설정값` 전달
    - 200 응답
    - 응답에 `csv`, `md` 필드 포함(base64 또는 data URL)
  - [ ] `csv` 필드를 base64 decode 했을 때:
    - 첫 줄에 헤더 `content_id,orig_tok,lex_tok,vec_tok,lex_cov,flags` 포함 테스트
  - [ ] `md` 필드를 decode 했을 때:
    - Markdown 테이블 혹은 summary 블록 형식이 유효한지(간단한 문자열 패턴으로) 테스트
  - [ ] rows가 비어있는 경우 → 빈 CSV/MD라도 정상 생성되는지 테스트
  - [ ] 잘못된 rows 구조(필수 필드 누락) → 4xx 응답 또는 Validation 에러 테스트
  ***
  ## 🟦 4. CLI 배치 평가 v05 (M19)
  ### M19 – `run_eval_v05` (LLM 연동)
  - [ ] `python -m app.eval.run_eval_v05 ...` 형태의 CLI 엔트리 구현
  - [ ] 입력 JSON 경로가 잘못된 경우 → 적절한 에러 메시지/exit code 테스트
  - [ ] LLM 요약 부분을 mock해서:
    - 100건 입력 시 전체 플로우 에러 없이 돌아가는지 테스트
    - CSV/MD 파일이 지정 경로에 생성되는지 테스트
  - [ ] CLI 옵션 테스트:
    - `-lexical-limit`, `-vector-limit`, `-model` 등이 내부 wrapper에 제대로 전달되는지 검증
  - [ ] 일부 문서만 실패하도록 mock했을 때:
    - 실패 문서가 flags에 `LLM_FAIL`로 표시되는지
    - 전체 실행이 중단되지 않고 끝까지 진행되는지 테스트
  ***
  ## 🟦 5. API 계약/통합 검증 (M20)
  ### M20 – Preview/Download API 계약 안정성
  - [] `/eval/preview` 응답 형태가 프론트에서 사용하는 필드 이름과 일치하는지(스냅샷/스키마 테스트)
  - [] `/eval/download` 응답을 프론트가 그대로 다운로드 링크로 사용할 수 있는지(콘텐츠 타입/포맷 테스트)
  - [] 작은 샘플(2~3건)로 preview → download 순서 호출 시:
    - preview rows 수와 download 결과 CSV row 수가 일치하는지 테스트
  - [] 비정상 요청(필수 필드 누락, 잘못된 타입)을 보냈을 때:
    - 500이 아닌 4xx로 떨어지는지, 에러 메시지가 최소한의 디버깅 정보를 제공하는지 테스트
  ***
  ## 🟩 6. ver05 체크리스트
  - [ ] M16 LLM 요약 wrapper + retry
  - [ ] M17 `/api/v1/eval/preview` 구현/테스트
  - [ ] M18 `/api/v1/eval/download` 구현/테스트
  - [ ] M19 CLI 배치 평가 v05 구현/테스트
  - [ ] M20 API 계약/통합 검증
  ***
  ## 🕒 ver05 4시간 작업 플랜
  ### 1시간 – LLM Wrapper
  - [ ] summarize_lexical/vector 기본 구현
  - [ ] retry/timeout/fallback 로직 + 유닛 테스트
  ### 1시간 – Preview API
  - [ ] `/eval/preview` 엔드포인트 구현
  - [ ] rows/summary 구조 유닛 테스트
  ### 1시간 – Download + Reporter 연동
  - [ ] `/eval/download` 구현
  - [ ] CSV/MD 생성 로직 재사용 + 테스트
  ### 1시간 – 통합 & CLI
  - [ ] run_eval_v05 CLI 작성
  - [ ] E2E(Preview/Download/CLI) 스모크 테스트 작성
