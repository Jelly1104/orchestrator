SELECT BOARD_IDX, CTG_CODE, TITLE, READ_CNT, AGREE_CNT, REG_DATE, (READ_CNT + AGREE_CNT * 3) as SCORE FROM BOARD_MUZZIMA WHERE REG_DATE >= NOW() - INTERVAL 24 HOUR ORDER BY (READ_CNT + AGREE_CNT * 3) DESC LIMIT 5