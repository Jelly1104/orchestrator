SELECT B.BOARD_IDX, B.CTG_CODE, B.TITLE, B.READ_CNT, B.AGREE_CNT, B.REG_DATE, (B.READ_CNT + B.AGREE_CNT * 3) AS popularity_score FROM BOARD_MUZZIMA B WHERE B.REG_DATE >= DATE_SUB(NOW(), INTERVAL 24 HOUR) ORDER BY popularity_score DESC LIMIT 5