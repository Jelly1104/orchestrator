SELECT C.BOARD_IDX, COUNT(*) as COMMENT_COUNT FROM COMMENT C WHERE C.BOARD_IDX IN (SELECT B.BOARD_IDX FROM BOARD_MUZZIMA B WHERE B.REG_DATE >= NOW() - INTERVAL 24 HOUR ORDER BY (B.READ_CNT + B.AGREE_CNT * 3) DESC LIMIT 5) AND C.REG_DATE >= NOW() - INTERVAL 24 HOUR GROUP BY C.BOARD_IDX LIMIT 1000