SELECT BOARD_IDX, CTG_CODE, TITLE, READ_CNT, AGREE_CNT, (READ_CNT + AGREE_CNT * 3) AS popularity_score, REG_DATE FROM BOARD_MUZZIMA WHERE REG_DATE >= NOW() - INTERVAL 24 HOUR ORDER BY popularity_score DESC LIMIT 5