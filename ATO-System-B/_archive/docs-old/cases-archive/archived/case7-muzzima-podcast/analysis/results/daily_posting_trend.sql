SELECT DATE_FORMAT(REG_DATE, '%H:00') as hour_slot, COUNT(BOARD_IDX) as post_count FROM BOARD_MUZZIMA WHERE REG_DATE >= NOW() - INTERVAL 24 HOUR GROUP BY DATE_FORMAT(REG_DATE, '%H:00') ORDER BY hour_slot