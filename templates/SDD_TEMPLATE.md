---
status: draft
created: YYYY-MM-DD
updated: YYYY-MM-DD
author: {작성자}
source: planning/PRD.md, design/Storyboard_REQ.md
---

# SDD (Software Design Document): {기능명}

---

## 1. 개요

### 1-1. 목적

{이 기술 설계 문서의 목적}

### 1-2. 범위

* **포함:** {이 SDD에서 다루는 범위}
* **제외:** {이 SDD에서 다루지 않는 범위}

### 1-3. 관련 문서

* PRD: `planning/PRD.md`
* Storyboard: `design/Storyboard_REQ.md`

---

## 2. 시스템 아키텍처

### 2-1. 전체 구조

```
[Client]
    ↓
[API Gateway]
    ↓
[{서비스명} Service]
    ↓
[Database / External Services]
```

### 2-2. 컴포넌트 구성

**Backend:**
* `{모듈명}`: {역할}
* `{모듈명}`: {역할}

**Frontend:**
* `{컴포넌트명}`: {역할}
* `{컴포넌트명}`: {역할}

---

## 3. 데이터 설계

### 3-1. 데이터 모델

**{엔티티명}**

```
{엔티티명}
├── id: UUID (PK)
├── {필드명}: {타입}
├── {필드명}: {타입}
├── created_at: datetime
└── updated_at: datetime
```

**{엔티티명}**

```
{엔티티명}
├── id: UUID (PK)
├── {외래키}_id: UUID (FK → {참조 엔티티})
├── {필드명}: {타입}
└── {필드명}: {타입}
```

### 3-2. ERD

```
[{엔티티 A}] 1 ──── N [{엔티티 B}]
                          │
                          N
                          │
                    [{엔티티 C}]
```

### 3-3. 인덱스

* `{테이블명}`: {인덱스 컬럼} - {목적}
* `{테이블명}`: {인덱스 컬럼} - {목적}

---

## 4. API 설계

상세 API 명세는 `development/API.md` 참조.

### 4-1. API 목록

* `POST /api/v1/{resource}`: {설명}
* `GET /api/v1/{resource}/{id}`: {설명}
* `PUT /api/v1/{resource}/{id}`: {설명}
* `DELETE /api/v1/{resource}/{id}`: {설명}

### 4-2. 인증/인가

* **인증 방식:** {JWT / Session / etc}
* **권한 체계:** {역할별 접근 권한}

---

## 5. 비즈니스 로직

### 5-1. {기능명}

**입력:** {입력 데이터}

**처리:**
1. {단계 1}
2. {단계 2}
3. {단계 3}

**출력:** {출력 데이터}

**예외 처리:**
* {예외 상황}: {처리 방법}

### 5-2. {기능명}

**입력:** {입력 데이터}

**처리:**
1. {단계 1}
2. {단계 2}

**출력:** {출력 데이터}

---

## 6. 외부 연동

### 6-1. {외부 서비스명}

* **목적:** {연동 목적}
* **방식:** REST API / SDK / etc
* **인증:** {인증 방식}
* **타임아웃:** {초}
* **재시도 정책:** {재시도 횟수 및 간격}

---

## 7. 성능 요구사항

* **응답 시간:** {목표 수치}
* **동시 처리량:** {목표 수치}
* **데이터 용량:** {예상 용량}

---

## 8. 보안 고려사항

* {보안 요구사항 1}
* {보안 요구사항 2}
* {민감 데이터 처리 방안}

---

## 9. 구현 계획

### 9-1. 파일 구조

```
src/
├── backend/
│   └── {기능명}/
│       ├── router.py
│       ├── service.py
│       ├── repository.py
│       └── models.py
└── web/
    └── {기능명}/
        ├── components/
        ├── hooks/
        └── pages/
```

### 9-2. 구현 순서

1. {단계 1}: {설명}
2. {단계 2}: {설명}
3. {단계 3}: {설명}

---

## 10. 관련 코드

* `src/backend/{경로}`
* `src/web/{경로}`
* `tests/{경로}`

---

## 11. 변경 이력

* **YYYY-MM-DD:** 최초 작성 ({작성자})
